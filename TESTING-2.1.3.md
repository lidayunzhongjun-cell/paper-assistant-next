# 2.1.3 无效关系容错测试

安装包：`dist/paper-assistant-next-2.1.3.xpi`。使用同一 Next 插件 ID，可直接覆盖升级 2.1.1/2.1.2；旧版 `zotero-paper-mind` 不受影响。

## 复测步骤

1. Zotero → 工具 → 插件 → 齿轮 → 从文件安装插件，选择 2.1.3 XPI，然后重启 Zotero。
2. 打开此前报告“关系指向不存在的段落或缺少说明”的论文，重新点击“建立全文知识图谱”。失败构建没有保存半成品，无需先清缓存。
3. 若模型再次给出无效关系，状态栏应显示“已忽略 N 条……”并继续后续章节串联，而不是终止建图。
4. 建图完成后展开段落关系：只应出现目标段落存在、不是自关联且带类型和依据的边。
5. 对照原文检查段落和摘要仍完整；容错只影响可选的模型推断关系，不应隐藏正文或关键引文错误。

若仍然失败，请发送状态栏的完整错误原文和失败阶段。不要提供 API Key。
