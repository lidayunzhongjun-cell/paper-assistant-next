# Paper Assistant Next 2.2.1 测试清单

## 自动验证

```powershell
npm test
npm run build
npm run check:zotero -- --local
```

预期：46 项测试通过；XPI 包含 20 个 KaTeX WOFF2 字体和许可证；打包启动冒烟及本机 Zotero 清单规则通过。

## Zotero 手工验收

1. 安装 `dist/paper-assistant-next-2.2.1.xpi` 并重新打开工作台。
2. 选择包含分式、上下标、希腊字母或矩阵的论文段落，点击“公式复原”。
3. 行内公式应和文字在同一行对齐；独立公式应居中；很宽的公式应在公式区域横向滚动而不撑破回答卡片。
4. 检查分子分母、上下标、求和/积分上下限、粗体向量、括号层级和公式编号是否与 PDF 一致。
5. 若选段提取文本已经缺失符号，回答应显示歧义说明，不应声称唯一复原。
6. 重新打开包含 LaTeX 的旧回答，确认无需重新请求 API 即可用新排版显示。

## 已知边界

插件仍以 PDF 提取文字为输入，尚未截取公式图片进行视觉识别。KaTeX 解决“已有 LaTeX 如何正确排版”，不能恢复提取阶段已经丢失且上下文无法唯一推断的符号。
