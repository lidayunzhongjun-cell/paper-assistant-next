# 2.1.0 本地 XPI 测试说明

安装包：`dist/paper-assistant-next-2.1.0.xpi`。适用范围沿用本机 Zotero 9（9.0–9.*）；同一 Next 插件 ID，手动安装会升级 Next。旧版 Paper Mind 使用不同 ID。

## 安装与实际论文测试

1. Zotero → 工具 → 插件 → 齿轮 → 从文件安装插件，选择本地 XPI。不要使用“导入文献”，也不要双击 XPI。
2. 打开一篇有可提取文字、章节清晰的短论文；工具 → Paper Assistant Next · 精读工作台。
3. 点击“建立全文知识图谱”。确认 API 地址及此次多次模型调用后开始。首次建立比旧版摘要更慢；模型输出格式错误会明确报错，不替换旧图谱。
4. 展开“全文精读 · 树状知识图谱”，对照原 PDF 核查章节—小节—段落的覆盖、顺序及边界；检查高/低重要性段落的摘要详略、重要性和密度判据、关键原文以及独立术语表。
5. 展开方法段，点击“精读 / 追问这一段”，询问“这个假设在哪里定义？实验和局限如何支持或限制它？”。核查状态经历图谱预判与原文串读，回答材料标记段落 ID，原文可在图谱节点中查看。
6. 选择回答中的一句话并引用追问，确认仍锁定同一论文和段落，同时保留最近的对话上下文。
7. 关闭并重新打开工作台，确认图谱、术语、问答均保留；复制完整图谱以检查 Markdown 导出。
8. 已有图谱时重新构建并中途停止，确认旧图谱仍可使用。首次构建中止不会保存半成品，下次需要重新构建。

## 已完成验证

- 29 项 Node 自动测试通过，包含模拟模型响应的完整建图/路由流程、完整文字覆盖、跨批段落延续、引文核验、节点关系校验、原文定位、预算、取消、降级及保存恢复。
- XPI 构建、压缩包结构和清单检查通过。
- 安装包内 bootstrap → startup → shutdown 在模拟 Zotero 宿主中通过。
- 从本机 `C:/Program Files/Zotero/omni.ja` 提取实际清单校验代码，验证新包通过、缺失 update_url 的对照包被拒绝。
- `git diff --check` 通过。

命令：

```powershell
npm test
npm run build
npm run check:zotero -- --local
```

## 尚未验证

尚未完成真实 Zotero 安装及真实模型论文解读。Windows 桌面自动化能读取 Zotero 控件，但截图报 `SetIsBorderRequired failed: 不支持此接口 (0x80004002)`，点击报 `coordinate input geometry is unavailable`，无法完成自动安装。没有使用用户 API Key 调用付费模型。

双栏 PDF、跨页段落、复杂公式和图表的实际表现需要人工核对；当前没有 OCR 或图表视觉理解。模型凝练及关系均可能有误，回答必须以检索原文核验。每轮图谱追问通常为一次路由加一次回答，节省用量和速度改善尚未实测；字符预算不等于 token 预算。

版本仅供本地测试，未发布到 GitHub，公共更新源没有加入不存在的下载地址。已有阅读记录沿用本地数据目录，未自动同步到 Zotero 云端。
