# 2.1.2 关联位置异常修复测试

安装包：`dist/paper-assistant-next-2.1.2.xpi`。适用于 Zotero 9.0–9.*，使用同一 Next 插件 ID，可直接覆盖升级 2.1.1；旧版 `zotero-paper-mind` 不受影响。

## 复测步骤

1. Zotero → 工具 → 插件 → 齿轮 → 从文件安装插件，选择 2.1.2 XPI。
2. 重启 Zotero，确认插件列表显示 2.1.2。
3. 打开在 2.1.1 中失败的同一篇 PDF，进入精读工作台。
4. 重新点击“建立全文知识图谱”并确认模型请求。失败的 2.1.1 构建不会保存半成品，因此无需先清缓存。
5. 检查结构识别越过之前失败的批次，不再出现“跨块段落延续与章节位置不一致”。
6. 建图完成后展开失败位置附近的章节、小节和段落，对照原文确认跨批段落没有重复、丢失或被放入新的伪章节。

如果仍失败，请记录工作台状态栏的**完整错误原文**、所用模型 ID、PDF 提取字符量和失败时显示的阶段；不要提供 API Key。不同错误（例如关键引文无法核验、关系引用未知段落）需要分别处理。

## 自动验证

- 38 项 Node 测试通过。
- 包含模型跨批标题从 `2 Methods / 2.1 Data` 漂移为 `Methods / Data collection` 的完整建图回归。
- 包含 1200 字符源单元及请求批次同时切开一条长文本行的强制连续回归。
- 构建后的 XPI 继续通过本机 Zotero 9.0.6 清单校验和打包后 bootstrap 冒烟测试。
