# 2.1.4 关键引文核验修复测试

安装包：`dist/paper-assistant-next-2.1.4.xpi`。可直接覆盖升级 2.1.1–2.1.3。

1. Zotero → 工具 → 插件 → 齿轮 → 从文件安装插件，选择 2.1.4 XPI并重启。
2. 打开此前出现“关键引文无法在对应段落中验证”的论文，重新建立全文知识图谱。
3. 若模型再次返回无法定位的引文，状态栏应提示忽略数量并继续建图。
4. 建图完成后抽查“关键原文”：显示内容必须能在对应段落原文中找到；模型改变过的空白和标点会恢复成 PDF 提取文字的原样。
5. 改写、翻译、拼接或编造的句子不应出现在关键原文列表中。

自动验证：40 项测试、XPI 完整性、打包后 bootstrap 冒烟测试和本机 Zotero 9.0.6 清单校验。
