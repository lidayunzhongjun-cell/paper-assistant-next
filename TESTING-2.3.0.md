# Paper Assistant Next 2.3.0 测试清单

## 自动验证

```powershell
npm test
npm run build
npm run check:zotero -- --local
```

预期：48 项测试通过；生成 `dist/paper-assistant-next-2.3.0.xpi`；打包启动冒烟和本机 Zotero 清单检查通过。

## 快速建图验收

1. 导入一份具有 Markdown 标题或“1. 方法”等编号标题的 DOCX/TXT/MD 精炼稿。
2. 先点击“本地建立精炼稿导航（0 Token）”，应立即完成且没有网络请求。
3. 再点击“AI 优化精炼稿图谱（1次 API）”；确认框应明确显示最多一次调用。状态应先显示“本机解析标题层级”，再显示“唯一一次 AI 优化”，不再循环显示逐章总结。
4. 建图结束后展开各节点，确认导入文档的开头、中部和末尾都可查看，没有因 API 预算而从本地丢失。
5. 将 API 地址临时改成不可用地址并重建：应在一次失败后保存本地导航，不自动继续请求。
6. 已有 PDF 图谱应保持不变，顶部仍可在 PDF 图谱和精炼稿图谱间切换。
7. 使用精炼稿图谱追问时，材料说明应显示“0 次路由调用”；API 只接收最终回答请求。

## 性能边界

- 本地导航模式：零次请求、零模型 Token，完整文字全部留在本机。
- AI 优化模式的普通精炼稿：一次请求读取完整精炼稿。
- 超长精炼稿：一次请求发送各导航单元的首尾样本，模型材料约束在 42,000 字符附近；完整文字只在本地保存和展示。
- 本测试保证请求次数和字符上界，不承诺不同 API 服务商的实际 Token 计费方式或响应时间。
